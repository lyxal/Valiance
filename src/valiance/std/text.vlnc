#?? Trim a string and append an exclamation mark.
#??
#?? @param value String to transform.
#?? @returns The trimmed string followed by `!`.
public define exclaim(value: String) -> String =>
  $value | std.text.trim | + "!"
end
