define trapezoidal(
  fn: Function[Number -> Number],
  a: Number,
  b: Number,
  n: Number
) -> Number =>
  $h = ($b - $a) / $n

  #? Get the sum of f(x1) to f(x_n-1).
  $fsum = sum $fn(range(1, $n - 1) * $h + $a)

  #? Final calculation.
  $h * sum [0.5 * $fn($a), 0.5 * $fn($b), $fsum]
end
