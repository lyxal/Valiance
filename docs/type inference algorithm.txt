If strategy =>

1. condition_env = Environment(self.branches)
2. post_condition_branches, condition_node = condition_env.analyse(node.condition)
3. body_env = Environment(post_condition_branches)
4. post_body_branches, body_node = body_env.analyse(node.body)
5. post_body_branches.broadcast_push(Optional(body_env.output()))
6. return <stuff>

end

"Where's the rest of the algorithm?"
In my Joplin notes.
