define[T, U] dotProd(
  left: trait =>
    extend +(:T, :T) -> T
    extend *(:T, :U) -> T
  end +,
  right: U+
) =>
  * | reduce: +
end

range(1, 100) map fn (n) => % [3, 5] == 0 dotProd swap ["Fizz", "Buzz"] or "$n"
println