[project]
name = "valiance"
version = "0.1.0"
description = "The Valiance Programming Language"
requires-python = ">=3.14"
dependencies = [
    "isort>=8.0.1",
]

[project.scripts]
valiance = "valiance.main:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"